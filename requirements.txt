# StayOn - 할 일 관리 앱
# Python 3.11+ 필수

# 핵심 패키지
streamlit==1.55.0           # 웹 앱 프레임워크
pandas==2.2.0               # 데이터 처리
plotly==5.18.0              # 차트 라이브러리

# 선택사항 (향후 기능 추가)
python-dotenv==1.0.0        # 환경변수 관리 (API 키, 토큰 등)
